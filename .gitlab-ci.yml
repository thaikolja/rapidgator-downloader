# The names and order of the pipeline stages
    image: docker:latest
    stages:
      - test
    test-job-example:
      stage: test
      script:
        - python3 --version
        - pip3 install requests